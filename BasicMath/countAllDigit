def countAllDigit(a: int ) -> int:
    cnt = 0 
    sing = 1 
    if a<0:
        sing = -1
    a = sing * a
    while(a > 0):
        n = a%10
        cnt = cnt +1
        a=a//10
    return cnt

print(countAllDigit(123))
